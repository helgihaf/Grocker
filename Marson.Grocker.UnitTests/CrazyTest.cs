using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using System.IO;
using System.Text;

namespace Marson.Grocker.UnitTests
{
    [TestClass]
    public class CrazyTest
    {
        //[TestMethod]
        //public void BlockingRead()
        //{
        //    const string filePath = @"C:\inetpub\logs\LogFiles\W3SVC1\u_ex150924.log";

        //    byte[] buffer = new byte[1024];
        //    int readBytes;
        //    using (var stream = new FileStream(filePath, FileMode.Open, FileAccess.Read, FileShare.ReadWrite | FileShare.Delete))
        //    {
        //        while (true)
        //        {
        //            readBytes = stream.Read(buffer, 0, buffer.Length);
        //            if (readBytes > 0)
        //            {
        //                Console.WriteLine(Encoding.UTF8.GetString(buffer, 0, readBytes));
        //            }
        //            else
        //            {
        //                Console.WriteLine("Blocking...");
        //            }
        //        }

        //    }

        //}
    }
}
